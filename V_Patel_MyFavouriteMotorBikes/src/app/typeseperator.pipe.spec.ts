import { TypeseperatorPipe } from './typeseperator.pipe';

describe('TypeseperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeseperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
